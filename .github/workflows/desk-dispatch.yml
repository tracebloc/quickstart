# GENERATED FILE -- DO NOT EDIT BY HAND.
#
# Self-contained copy of the reusable workflow `.github/workflows/desk-dispatch.yml`
# (blob bdb288c08131d6411197ecdaacf3caa66fc5d1ff) folded with the private source repo's own caller of it,
# `.github/workflows/desk-dispatch-caller.yml` (blob a79f6adf63a69f5f01c29b70b803ab72c8451104), for a PUBLIC repo: GitHub lets a
# public repo `uses:` a reusable workflow only from a public repo, and the
# reusables live in the private source repo. So this file carries the
# reusable's jobs inline -- the triggers are the caller's; every `inputs.*` is
# resolved to a literal (repo-inventory.yml's `caller_inputs` floor, else the
# reusable's default -- the caller's own toolchain choices are NOT copied); the
# App-token secrets are named directly; a job whose condition resolves to a
# constant false is omitted; and each job's display name keeps the
# `desk / ` prefix so the check contexts branch protection requires do
# not change. Scripts are still fetched from the private source repo at run time with a
# read-only App installation token; on a fork PR that token cannot be minted
# (no secrets), so the checkout fails closed rather than running untrusted input.
# Where the copy checks the private source repo out, its `tools-ref` is the literal `main` --
# the ref every caller of the reusable pins -- because a public copy has no
# reusable to take the commit from (the private original defaults to its own).
#
# PUBLIC SURFACE. This file is delivered to a public repo. Exactly what was
# scrubbed, and what was not:
#
#   * COMMENT TEXT -- a whole-line YAML `#` comment, a `#`-first line of a `run:`
#     script that is not inside a heredoc, and this header -- was scrubbed: every
#     private-repo issue reference, private repo name (org-qualified OR bare),
#     RFC id and tenant name became `(internal ref)`, and the private repo this
#     file was generated from became `the private source repo`.
#     A bare name is left alone where the next character is `/` or `#`: that is a
#     path or branch prefix, or the issue form, which is matched on its own.
#     A name that is also an ordinary English word, declared as such on the
#     inventory row, is left alone in prose as well: it is scrubbed only inside a
#     code span, as `<org>/<repo>` (URL or not), or as `<repo>#N`.
#   * CODE -- everything else: commands, heredoc bodies, strings a workflow posts,
#     `repository:` values, URLs, step names -- was NOT edited. An internal
#     reference there refuses the render (it is a defect in the source). A BARE
#     private repo name is never judged in code at all: several are also public
#     product identifiers -- an image a public chart installs, a package name, a
#     branch prefix -- so only the org-qualified spelling refuses there, with one
#     exception: the name of the private repo this file was generated from, which
#     the code states verbatim because the workflow checks that repo out and
#     fetches its scripts from it at run time.
#
# Regenerate:  python3 scripts/render-self-contained.py          (in the private source repo)
# Verify:      python3 scripts/render-self-contained.py --check  (make selftest-self-contained)
#
# The comments below are the reusable's own and describe the reusable; where
# they mention `inputs.*` or callers, read them as history.

# desk-dispatch: fire ONE fresh code-review-desk session for ONE pull request, only
# when GitHub says a review is wanted ((internal ref) and (internal ref)).
#
# WHY. The desk used to be a five-minute cron inside a long-lived session that
# re-read ~500k tokens on every wake -- measured over 14 days, the single largest
# Claude Code cost on its machine. This reusable is the cheap shape: no model runs
# until an event arrives, then one session handles one PR. The workflow itself
# costs a few seconds of `ubuntu-slim`, billed as a whole minute -- which is why a
# no-op run holds its concurrency slot to the end of that minute while a check is
# still running ((internal ref) D6 lever 1; the stamp step below).
#
# WHO REVIEWS. `reviewer` is the identity whose review requests count and whose
# routine fires. It is a dedicated review account, not a person: the routine runs
# as that account (self-review mode in the skill, `CRC_SELF_REVIEW=1`), so the
# staffing pass requests it on every PR it may review and the review lands as
# that account. A person's login here would make their name stand in for a bot
# on every approval, and could never cover their own PRs.
#
# WHAT IT FIRES ON (the decision lives in scripts/desk-dispatch.py, tested offline):
#   review requested from `reviewer`                   -> a review session, IF the
#                                                          head is green
#   push to a PR `reviewer` has reviewed / is on       -> a re-review session, IF green
#   workflow_run completed (a CI workflow): every CI run and commit status on the
#     head terminal and green, `reviewer` requested or already on the PR, head
#     not yet reviewed                                    -> a review session
#   a run or status still running / any red / mergeable_state dirty
#                                                       -> skip (the last one to
#                                                          finish fires; red waits
#                                                          for the author's fix)
#   ... the ONLY red is `bugbot / review` and the gate  -> HEAL: re-run THAT gate run
#       would pass if evaluated now (its threads were      in place, as the App; no
#       resolved after it ran -- (internal ref))             session; its completion is
#                                                          the next event
#   a GitHub Actions check_suite                        -> skip BEFORE any read
#   PR opened or marked ready, non-draft               -> NO session: this job requests
#                                                          `reviewer` AS THE APP and
#                                                          assigns the author; the
#                                                          review_requested that raises
#                                                          fires the review
#   ... with `reviewer` already requested              -> a review session, IF the head
#                                                          is green (a no-op request
#                                                          raises nothing; gated like
#                                                          every other review)
#
# WHY THE REVIEWER REQUEST IS MADE WITH AN APP TOKEN. GitHub starts no workflow run
# for an event raised by GITHUB_TOKEN (post-release-bump.yml measured the same
# recursion guard), so a request made with `github.token` would land and raise a
# `review_requested` no caller sees. A second installation token, scoped to this
# caller repo and pull-requests: write, is minted for that ONE call; label writes
# and every read stay on `github.token`. Drafts, bots, promotion heads, team
# requests and the reviewer's own PRs are skipped with the reason in the job summary.
#
# THE GREEN GATE (Lukas, 2026-09-14). The review account is the most expensive
# check on a PR, so nothing fires until the head is green; Bugbot is part of the
# rollup (bugbot-gate) and review latency becomes CI latency, by design. What
# "green" means is `head_state()` in scripts/desk-dispatch.py, shared with
# ship-dispatch.py: every CI check run and commit status terminal and none red,
# the dispatchers' own `... / dispatch` runs excluded; `mergeable_state` is read
# for `dirty` only.
#
# WHY workflow_run AND NEVER check_suite. The caller's own run is a GitHub Actions
# check suite, and a `check_suite` trigger cannot filter by app, so a caller on
# `check_suite: [completed]` re-triggers itself for ever (Bugbot on (internal ref)
# and (internal ref)); `workflow_run` names the CI workflows it follows and cannot
# name itself. Callers MUST send `workflow_run: {workflows: [<every pull-request
# workflow of that repo>], types: [completed]}` and grant `checks: read` +
# `statuses: read`. THE GATE DEGRADES PER CALLER: a caller from before the gate
# grants neither read, so a 403 on the head read fires the review UNGATED with the
# missing grants named in the job summary ((internal ref)) -- never a red run in
# ten repos. A pull-request workflow the caller does not list leaves the review
# unfired when it finishes last ((internal ref), (internal ref)).
#
# NO `permissions:` BLOCK IN THIS REUSABLE, DELIBERATELY. A reusable's declared
# permissions are a DEMAND on the caller, not a grant: a caller granting less is
# a `startup_failure` with no job, no log and no summary -- every desk run from a
# pre-gate caller ended that way for fourteen hours after (internal ref) promoted,
# and the degrade path in the script never started. Without a block the job's
# token carries exactly the caller's grants and the shortfall reaches the script
# as the 403 the degrade path was written for.
# `test_the_reusables_declare_no_permissions_block` pins this.
#
# THE LOCK. `claude-desk:reviewing` on the PR means a session is on it; a second
# review event while it is held is skipped (the hourly `--once` sweep catches up).
# The session removes the label when it finishes (skill `--pr` mode, step 4); a
# label older than `stale-minutes` is a dead session's and is stolen.
#
# THE OTHER LOCK. `claude-ship:driving` is the AUTHOR's: a review fired while it
# is held reviews a head the author is replacing (45 % of desk lock windows
# overlapped a live ship lock and 12 of the 13 that filed no review sat inside
# that overlap -- (internal ref)). The dispatcher DEFERS on it, held only, capped
# at `ship-stale-minutes` wherever the label's age is readable ((internal ref) for
# the unreadable case), takes NO lock while deferring, and re-enters on the
# caller's `unlabeled` type. Ship never reads the desk's lock, so nothing deadlocks.
#
# FAIL CLOSED. An unreadable event, an unreadable reviews/events read, an empty
# fire URL or token when there is work to do, or a fire the routine refuses are
# refusals (red job, message names the input), never a silent skip (CLAUDE.md
# rules 3, 8). This job is NOT a required context anywhere, so a red run blocks
# nothing; it is the operator's signal that reviews stopped flowing. THE ONE
# NON-2xx THAT IS NOT A REFUSAL is the routine's own rate limit ((internal ref)):
# waited when the interval fits the budget, otherwise DEFERRED -- lock released,
# `::warning::`, run green -- because the 20-minute sweep re-fires it.
#
# THE CANONICAL CALLER is `desk-dispatch-caller.yml` beside this file
# ((internal ref)); a caller calls `@main`, so any edit here a caller depends on
# must be promoted before a caller relies on it ((internal ref)). The caller passes
# `with.fire-url: ${{ vars.DESK_ROUTINE_FIRE_URL }}` and the three named secrets
# below, and nothing else: `reviewer` is derived from the default, never
# restated. The pilot repo's caller diverged from the canonical one at
# (internal ref) (no `unlabeled` type); the canonical caller's header records
# that window and why no check sees it.
#
# THE INVENTORY ROWS for this file flip from `exempt` to `required` on the
# condition `repo-inventory.yml`'s `desk_dispatch_rollout_pending` anchor states
# -- the pilot OBSERVED dispatching on real PRs -- caller first, row after,
# because the audit reads the caller from the live branch.
#
# THE CALLER'S OWN TOKEN writes the label, reads reviews/events in its own repo,
# and reads the head's check runs and commit statuses for the gate
# (`pull-requests: write`, `issues: write`, `checks: read`, `statuses: read`, on
# `contents: read`) -- GRANTED BY THE CALLER, never declared here. The App token
# is minted ONLY to read this private repo's checker (`permission-contents: read`
# on the private source repo); the routine token and fire URL come from the caller.
#
# NO `paths:` FILTER may ever be added by a caller; NO `cancel-in-progress` here
# (reusable-no-cancel: a cancelled corpse on an unchanged sha reddens the rollup).
name: Desk dispatch

on:
  pull_request:
    types: [opened, ready_for_review, review_requested, synchronize, unlabeled]
  # NO BASE FILTER HERE ((internal ref), (internal ref)): `on:` filters a BASE ref
  # only and a base-ref filter would also drop an emergency hotfix into `main`,
  # so the promotion predicate can only live in the callee's `if:`.
  # THE GREEN GATE (Lukas, 2026-09-14): a review fires only once the head is green,
  # and a CI workflow completing is the event that carries that. The list names
  # EVERY workflow of THIS repo that runs on a pull-request event -- a completion
  # left off the list that finishes last leaves the review UNFIRED, nothing
  # re-fires ((internal ref), (internal ref)). The set is DERIVED:
  # `test_the_canonical_caller_is_gated_and_subscribes_to_no_check_suite` reads
  # this repo's workflow files and reddens on a missing or an extra name. NEVER
  # `check_suite` for the Actions app: the caller's own run is such a suite.
  workflow_run:
    # A `workflow_run`'s `branches` filter is on the TRIGGERING run's head branch
    # -- a PR run's feature branch -- so integration branches are IGNORED rather
    # than `develop` allowed: that drops the `push` and cron completions ((internal ref)).
    branches-ignore: [develop, staging, main]
    workflows:
      - Add to engineer kanban
      - Bugbot review gate
      - Code quality
      - Commit hygiene
      - FR gate
      - Model-zoo paths
      - Route kanban Status on closure
      - Set PR card Status on project
      - Template rules
    types: [completed]

permissions:
  contents: read
  pull-requests: write
  issues: write
  checks: read
  statuses: read

# One group per PR for EVERY event kind: a workflow_run / check_suite event carries
# the PR under `<event>.pull_requests[0].number`. Keying them differently would let
# a request and a finishing run on one PR execute in parallel and both read the
# lock label as free (the defect ship-dispatch.yml had, (internal ref)).
concurrency:
  group: desk-dispatch-${{ github.workflow }}-${{ github.event.pull_request.number || github.event.workflow_run.pull_requests[0].number || github.event.check_suite.pull_requests[0].number || github.event.workflow_run.head_sha || github.event.check_suite.head_sha }}
  cancel-in-progress: false

# NO `permissions:` BLOCK IN THIS REUSABLE, DELIBERATELY -- see the header. (A
# self-contained render DOES carry one: there caller and callee are one file, so
# the block that lands is the caller's grant -- tracebloc/quickstart#16.)

jobs:
  dispatch:
    name: desk / dispatch
    runs-on: ubuntu-slim
    timeout-minutes: 10
    # NOT ON A PROMOTION PR ((internal ref)): a promotion PR's head sha IS a
    # branch tip, and the runs every promotion queued here left `cancelled`
    # check-runs on it that hop-verify.sh reads as a VOID outranking every green
    # verdict -- the staging hop went from 16 repos verified to 0 of 18. Skipping
    # is the whole fix: the desk strips reviewers off promotions, and a job
    # skipped by `if:` concludes `skipped`, which hop-verify.sh allowlists.
    #
    # BOTH HALVES ARE REQUIRED, AND THE BASE REF ALONE IS NOT THE RULE
    # ((internal ref)): (internal ref)'s `scripts/hotfix-repo.sh` opens a PR into
    # `main` for an emergency fix, so dropping on a base-ref match alone drops a
    # hotfix with no reviewer staffed. The predicate is a TRAIN BRANCH going INTO
    # a promotion branch; `release-train/` rather than `release-train/to-` because
    # the train owns that namespace. BOTH HALVES SPELL `is_promo` EXACTLY --
    # `(release-train/* OR hotfix-backmerge/*) AND base in (staging, main, master)`
    # in scripts/desk-dispatch.py -- and scripts/tests/callershape.py
    # `assert_promotion_guard` evaluates THIS expression over every (head, base)
    # of its matrix against the real `is_promo`. An event shape carrying neither
    # ref yields nulls and the job RUNS: a wasted minute beats an unreviewed PR.
    if: >-
      !((startsWith(github.event.pull_request.head.ref
      || github.event.workflow_run.head_branch
      || github.event.check_suite.head_branch, 'release-train/')
      || startsWith(github.event.pull_request.head.ref
      || github.event.workflow_run.head_branch
      || github.event.check_suite.head_branch, 'hotfix-backmerge/'))
      && contains(fromJSON('["staging", "main", "master"]'),
      github.event.pull_request.base.ref
      || github.event.workflow_run.pull_requests[0].base.ref
      || github.event.check_suite.pull_requests[0].base.ref))
    steps:
      # WHEN THIS JOB STARTED, for the hold the dispatcher takes on a no-op
      # ((internal ref) D6 lever 1): a job bills a rounded-up minute, and a "still in
      # progress" verdict exits at ~20 s while every sibling completion starts
      # another run, so the script holds its slot until ~45 s after this stamp
      # (ABSORB_UNTIL_SECONDS in scripts/desk-dispatch.py) and a burst of N
      # completions costs two billed runs instead of about N/2, dropping nothing
      # (the caveat for `unlabeled` runs is in desk-dispatch-caller.yml's header,
      # (internal ref)). First, before the mints; a caller without this step gets no hold.
      - name: Note when this job started
        id: started
        run: echo "at=$(date +%s)" >> "$GITHUB_OUTPUT"
      - name: Mint a read-only token for org-config (private; the caller's own token cannot read it)
        id: org-config-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.RELEASE_TRAIN_APP_ID }}
          private-key: ${{ secrets.RELEASE_TRAIN_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: org-config
          permission-contents: read
      # SCOPED to the caller repo and to pull-requests: write, for exactly one
      # call: the reviewer request on a staff decision (see the header) -- an
      # event raised by this token starts the caller's `review_requested` run.
      # Only the two event shapes that can produce a `staff` decision mint it.
      - name: Mint a pull-requests token for the reviewer request (an App event fires a run; github.token's does not)
        id: request-token
        if: github.event_name == 'pull_request' && (github.event.action == 'opened' || github.event.action == 'ready_for_review')
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.RELEASE_TRAIN_APP_ID }}
          private-key: ${{ secrets.RELEASE_TRAIN_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ github.event.repository.name }}
          permission-pull-requests: write
      # SCOPED to the caller repo and to actions: write, for exactly one call: the
      # in-place re-run of a STALE `bugbot / review` run ((internal ref);
      # `heal_stale_gate` in scripts/desk-dispatch.py), as the App so the re-run's
      # completion is a `workflow_run` the caller sees. `continue-on-error`,
      # DELIBERATELY: a refused mint must not redden every dispatch, and the
      # script refuses ONLY when it finds a stale red it could have healed. Minted
      # on EVERY event this dispatcher acts on, the condition naming no action:
      # an action list here would be a second copy of the script's arms (a
      # staffing event can meet a recorded verdict too, via `decide_ungated`).
      - name: Mint an actions token for re-running a stale Bugbot gate in place (tolerates a refused mint)
        id: rerun-token
        if: github.event_name == 'workflow_run' || github.event_name == 'pull_request'
        continue-on-error: true
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.RELEASE_TRAIN_APP_ID }}
          private-key: ${{ secrets.RELEASE_TRAIN_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ github.event.repository.name }}
          permission-actions: write
      # A FOURTH scoped mint, for exactly ONE read: the base branch's
      # required-status-context list, so a red check that is NOT required stops
      # holding the head while a red required one still does ((internal ref);
      # `gate_required` / `required_contexts` in scripts/desk-dispatch.py) --
      # this repo's own fleet audits report on OTHER repos' live state, so one
      # repo drifting anywhere turned an inventory PR here red and the desk
      # stopped reviewing the PR that fixes the drift.
      #
      # SCOPED TO THE CALLER'S OWN REPO, AND READ-ONLY, and that narrowness is
      # the whole justification: an org-wide `actions: write` mint on the sweep
      # was proposed and closed unmerged, and `administration: write` is the
      # grant that could change protection. NO NEW ORG GRANT IS NEEDED: another
      # workflow already mints `permission-administration: read` from the same
      # App and reads a protection list byte-identical to an admin read (its run
      # is named on the PR that wired this, not here -- this file is delivered to
      # PUBLIC repos). A SEPARATE MINT, so a permission the installation stops
      # holding degrades this one feature instead of taking the dispatcher down;
      # `continue-on-error`, DELIBERATELY, for the same reason: a refused mint
      # leaves the token EMPTY and `gate_required` returns None without calling,
      # so every red holds the head -- the fail-CLOSED direction (rule 3). NO
      # `if:` CONDITION: both caller triggers reach a `gate_required` call.
      - name: Mint a protection-read token for the base branch's required contexts (tolerates a refused mint)
        id: protection-token
        continue-on-error: true
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.RELEASE_TRAIN_APP_ID }}
          private-key: ${{ secrets.RELEASE_TRAIN_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: ${{ github.event.repository.name }}
          permission-administration: read
      # THE TOOLS REF ((internal ref) D4, (internal ref)): `job.workflow_sha` is the commit
      # of THIS reusable file, so the scripts below are the ones it was reviewed
      # with; `tools-ref` overrides it for a test. An EMPTY ref would check out the
      # tools repo's DEFAULT branch fleet-wide, so it is refused. `toJSON(job)` because
      # actionlint's `job` schema (1.7.12) does not know `workflow_sha`.
      - name: Resolve the tools ref, refusing an empty one
        id: tools-ref
        env:
          TOOLS_REF: ${{ 'main' }}
          JOB_CONTEXT: ${{ toJSON(job) }}
        run: |
          if [ -z "$TOOLS_REF" ]; then
            case "$JOB_CONTEXT" in
              *'"workflow_sha"'*) TOOLS_REF="${JOB_CONTEXT#*\"workflow_sha\": \"}"; TOOLS_REF="${TOOLS_REF%%\"*}" ;;
            esac
          fi
          if ! [[ "$TOOLS_REF" =~ ^[0-9a-f]{40}$ || ( -n "$TOOLS_REF" && "$TOOLS_REF" != *[![:alnum:]._/-]* ) ]]; then
            echo "::error::the tools ref is empty or not a ref (${TOOLS_REF:-<empty>}); an empty ref would check out the tools repo's default branch instead of the commit this workflow was pinned at. Refusing."
            exit 1
          fi
          echo "ref=$TOOLS_REF" >> "$GITHUB_OUTPUT"
          echo "tools ref: $TOOLS_REF"
      - name: Check out the dispatcher
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: tracebloc/org-config
          token: ${{ steps.org-config-token.outputs.token }}
          ref: ${{ 'main' }}
          path: .org-config-tools
          persist-credentials: false
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
      - name: Decide, lock, fire
        env:
          GH_TOKEN: ${{ github.token }}
          DISPATCH_T0: ${{ steps.started.outputs.at }}
          DESK_REQUEST_TOKEN: ${{ steps.request-token.outputs.token }}
          DESK_RERUN_TOKEN: ${{ steps.rerun-token.outputs.token }}
          DESK_PROTECTION_TOKEN: ${{ steps.protection-token.outputs.token }}
          DESK_REVIEWER: ${{ 'tracebloc-review' }}
          DESK_FIRE_URL: ${{ (vars.DESK_ROUTINE_FIRE_URL) }}
          DESK_ROUTINE_TOKEN: ${{ secrets.DESK_ROUTINE_TOKEN }}
          DESK_STALE_MINUTES: ${{ 45 }}
          SHIP_STALE_MINUTES: ${{ 90 }}
        run: python3 .org-config-tools/scripts/desk-dispatch.py
