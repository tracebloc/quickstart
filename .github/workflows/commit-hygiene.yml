# GENERATED FILE -- DO NOT EDIT BY HAND.
#
# Public-safe copy of `.github/workflows/commit-hygiene.yml` (blob 1f019314b3e799375e63e3ebd671617cb4698942) for a PUBLIC repo:
# the same workflow, byte for byte, except that its comments are scrubbed as
# described below. Private repos carry the original; the conformance audit
# compares a public repo's copy against THIS file and a private repo's
# against the original.
#
# PUBLIC SURFACE. This file is delivered to a public repo. Exactly what was
# scrubbed, and what was not:
#
#   * COMMENT TEXT -- a whole-line YAML `#` comment, a `#`-first line of a `run:`
#     script that is not inside a heredoc, and this header -- was scrubbed: every
#     private-repo issue reference, private repo name (org-qualified OR bare),
#     RFC id and tenant name became `(internal ref)`, and the private repo this
#     file was generated from became `the private source repo`.
#     A bare name is left alone where the next character is `/` or `#`: that is a
#     path or branch prefix, or the issue form, which is matched on its own.
#     A name that is also an ordinary English word, declared as such on the
#     inventory row, is left alone in prose as well: it is scrubbed only inside a
#     code span, as `<org>/<repo>` (URL or not), or as `<repo>#N`.
#   * CODE -- everything else: commands, heredoc bodies, strings a workflow posts,
#     `repository:` values, URLs, step names -- was NOT edited. An internal
#     reference there refuses the render (it is a defect in the source). A BARE
#     private repo name is never judged in code at all: several are also public
#     product identifiers -- an image a public chart installs, a package name, a
#     branch prefix -- so only the org-qualified spelling refuses there, with one
#     exception: the name of the private repo this file was generated from, which
#     the code states verbatim because the workflow checks that repo out and
#     fetches its scripts from it at run time.
#
# Regenerate:  python3 scripts/render-self-contained.py          (in the private source repo)
# Verify:      python3 scripts/render-self-contained.py --check  (make selftest-self-contained)

name: Commit hygiene

# A COPY, not a reusable (`copies:` in repo-inventory.yml, beside add-to-kanban.yml):
# the public content repos carry the public-safe render of this file byte for byte
# under .github/workflows/, delivered by scripts/onboard_repo.py and content-asserted
# by scripts/caller-drift.py. Private repos keep this original. (internal ref).
#
# WHY THIS EXISTS. The org ruleset `content-repos-commit-hygiene` on the
# `exposure: content` repos refused customer names in commit messages by NAMING
# them in its regex -- and GitHub serves a public repo's branch rules to an
# anonymous reader, parameters included, so the names the org must never publish
# were published as the rule meant to keep them private. The regex is generic now
# (cross-repo `name#N`, org-qualified repo names, RFC ids), which left the tenant
# half with no enforcement. This workflow is that half, moved to where the needles
# stay secret: they are read from the same org secret that feeds the weekly
# anonymous exposure audit's tenant scan, parsed by scripts/public_surface.py, and
# a hit is reported by the needle's NUMBER only -- never the name, never the
# matched text. The logic is scripts/commit-hygiene.py; this file is the wiring.
#
# WHERE IT RUNS. Only where the commit log is a public surface: the job is gated
# on `github.event.repository.private == false`, so the private repo that holds
# this canonical skips it (a tenant name in a private commit log is nobody's
# business), and every public repo that carries the copy runs it. Arm it as a
# required status check on the content repos' `develop` once it has been observed
# green there (CLAUDE.md rule 4: never land a red gate).
#
# FAIL CLOSED (CLAUDE.md rule 3), and the script says which way:
#   exit 0  every commit scanned, clean
#   exit 1  a hit -- `commit <sha7>: subject/body matches private needle #N`
#   exit 2  could not tell: a fork head (no secrets), an absent or empty secret
#           ("tenant needles not configured -- the scan did not run"), more
#           commits than GitHub's endpoint lists (250, refused before any read),
#           a list that does not match the event's count, or a failed read.
# A fork PR is refused in the FIRST step below rather than by the script: the
# script is fetched from the tools repo with an App token minted from secrets a
# fork head does not receive, so on a fork the mint would fail first and the log
# would name a missing input instead of the cause. The script repeats the check
# for the shape where it is reached (HEAD_IS_FORK), and the suite proves that one.
#
# PULL-REQUEST-CONTROLLED INPUT NEVER REACHES A SHELL. The script reads the
# commit messages from the API into Python and matches them there; nothing from a
# commit message is interpolated into a command line or printed.

on:
  pull_request:
    types: [opened, synchronize, reopened]

# One run per pull request head: a new push makes the previous scan moot.
concurrency:
  group: commit-hygiene-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# `pull-requests: read` is what the one `gh api .../pulls/N/commits` read needs;
# `contents: read` is for nothing this workflow does in the calling repo (the
# tools checkout uses its own token) and is granted so `github.token` is not a
# zero-permission token, which some API paths reject outright.
permissions:
  contents: read
  pull-requests: read

jobs:
  commit-messages:
    name: hygiene / commit-messages
    # Public surfaces only -- see WHERE IT RUNS above. `github.event.repository`
    # is the repo the PR targets, which is the repo whose log is public.
    if: ${{ !github.event.repository.private }}
    runs-on: ubuntu-slim
    timeout-minutes: 5
    env:
      # The event facts the script holds the API read to. Read here, once, from
      # the payload -- not re-fetched -- so a PR the endpoint cannot list in full
      # is refused before any call is made.
      HEAD_IS_FORK: ${{ github.event.pull_request.head.repo.fork }}
      PR_COMMITS: ${{ github.event.pull_request.commits }}
    steps:
      - name: Refuse a fork head before the mint below fails on it
        if: ${{ github.event.pull_request.head.repo.fork }}
        run: |
          set -euo pipefail
          echo "::error::commit hygiene: COULD NOT TELL -- the pull request head is a FORK, and a fork PR receives no secrets, so the tenant needles cannot be read and the scan did not run. A maintainer reads the commit messages by hand before merging."
          exit 2

      # The checker lives in the tools repo, which is PRIVATE, so the checkout
      # needs a read-only App token -- the same mint every self-contained render
      # makes, scoped to that one repository and to `contents: read`.
      - name: Mint a read-only token for the tools repo
        id: org-config-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ secrets.RELEASE_TRAIN_APP_ID }}
          private-key: ${{ secrets.RELEASE_TRAIN_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: org-config
          permission-contents: read

      # `main` is the ref every fleet caller pins the tools repo at; a copy has no
      # reusable commit to take instead.
      - name: Check out the shared checker
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: tracebloc/org-config
          token: ${{ steps.org-config-token.outputs.token }}
          ref: main
          path: .org-config-tools
          persist-credentials: false

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      # Nothing is pip-installed: the checker and the module it imports use only
      # the standard library and shell out to `gh`, preinstalled on the runner.
      - name: Scan the pull request's commit messages against the tenant needles
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          # The tenant needles, one regex per line, from the org secret the
          # anonymous exposure audit already reads. An EMPTY value is exit 2, not
          # green: an unscanned log is not a clean one. The secret must be made
          # available to the public repos that carry this copy.
          ORG_CONFIG_TENANT_NEEDLES: ${{ secrets.PUBLISH_FORBIDDEN_TENANTS }}
        run: python3 .org-config-tools/scripts/commit-hygiene.py
